import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'id.shiorilabs.preflightx',
  appName: 'preflightx',
  webDir: 'out',
  bundledWebRuntime: false
}

export default config
