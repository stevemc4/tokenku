import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'id.shiorilabs.tokenku',
  appName: 'Tokenku',
  webDir: 'out',
  bundledWebRuntime: false
}

export default config
